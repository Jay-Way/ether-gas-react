export enum CryptoStatsL2OptionsEnum {
    arbitrum = 'arbitrum-one',
    optimism = 'optimistic-ethereum',
}

export enum L2OptionsEnum {
    arbitrum = 'arbitrum',
    optimism = 'optimism',
}

export enum TransferTypeOptionsEnum {
    transfer = 'transfer',
    swap = 'swap',
    erc20 = 'erc20',
}

export enum CryptoStatsQueriesEnum {
    transfer = 'feeTransferEth',
    swap = 'feeSwap',
    erc20 = 'feeTransferERC20',
}